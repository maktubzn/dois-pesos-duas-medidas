# Proxima Fatia Recomendada

Continuar a partir da migracao React/Vite aprovada.

## Prompt curto

Implementar Fatia 03: criar fluxo real de quiz em React usando `useGameStore`, controlar intro, nova pergunta, reveal apos cue do video, timer de resposta, travamento por `BT1PRESS`/`BT2PRESS`, reset por `RESET`, e pontuacao por grupo. Preservar visual atual, sem backend, sem Arduino fisico obrigatorio, e validar com `npm run typecheck`, `npm run build`, `npm run test -- --run` e `npm run test:e2e`.

